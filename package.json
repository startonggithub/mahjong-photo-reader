{
  "name": "mahjong-firebase-photo-reader",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static browser-only Mahjong tile photo reader using Firebase AI Logic",
  "scripts": {
    "check": "node --check app.js && node --check firebase-config.js",
    "serve": "npx serve . --listen 5500"
  }
}
